
import { currentUser } from 'dirty-water-front-end/helpers/current-user';
import { module, test } from 'qunit';

module('Unit | Helper | current user');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currentUser([42]);
  assert.ok(result);
});

